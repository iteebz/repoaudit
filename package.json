{
  "name": "repoaudit",
  "version": "0.1.0",
  "description": "AI-powered repo security & quality audit — run in any git repo, get actionable findings in seconds",
  "main": "dist/index.js",
  "bin": {
    "repoaudit": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "prepare": "npm run build",
    "dev": "ts-node src/index.ts",
    "test": "jest",
    "prepublishOnly": "npm run build"
  },
  "files": ["dist/", "README.md"],
  "keywords": ["security", "audit", "code-quality", "ai", "cli", "git", "static-analysis"],
  "author": "iteebz",
  "license": "MIT",
  "dependencies": {
    "openai": "^4.104.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0",
    "ts-node": "^10.9.2",
    "jest": "^29.0.0",
    "@types/jest": "^29.0.0",
    "ts-jest": "^29.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
